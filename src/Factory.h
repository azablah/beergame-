#include <iostream>
#include "Game.h"

using namespace std;


class Factory{

    private:
        int productionTime;

    public:

        //empty constructor
        Factory(){};

        int produce(int numberOfBeers){
			weekToBeProduced = currentWeek + productionTime;
			while (currentWeek = weekToBeProduced) {
				produce = numberOfBeers;
			}
            return produce;
        }

};
